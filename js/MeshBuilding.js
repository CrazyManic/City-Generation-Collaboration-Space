const directory = "Models/";

class MeshBuilding extends Building {
    Generate(urbanness, pColor1, pColor2, pColor3, mesh_files){
        meshes = [];
        for (var i=0; i < mesh_files.size; i++){
            
        }
    }
}